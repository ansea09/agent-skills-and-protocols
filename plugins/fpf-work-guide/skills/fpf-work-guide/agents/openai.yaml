interface:
  display_name: "FPF Work Guide"
  short_description: "Use current FPF context with refresh gate"
  default_prompt: "Use $fpf-work-guide to run the FPF context refresh gate, then answer with current FPF chunks, specification fallback, and Codex protocols."
